import type { Scanner } from '../types.js';
import { pass, warn } from '../result.js';
import { isWindows, runPowerShellJson } from '../../services/powershell.js';
export const tpmScanner: Scanner = { id: 'tpm', name: 'TPM', description: 'Checks TPM presence and readiness.', async run() { if (!isWindows()) return warn(this.id,this.name,'TPM status requires Windows PowerShell.','Check TPM on the Windows gaming PC.',{ platform: process.platform }); const { value, raw } = await runPowerShellJson<Record<string, unknown>>('Get-Tpm | Select-Object TpmPresent,TpmReady,TpmEnabled,TpmActivated,ManufacturerIdTxt,ManufacturerVersion',{},15000); const ready=value?.TpmPresent===true && value?.TpmReady===true; return raw.exitCode===0 && ready ? pass(this.id,this.name,'TPM is present and ready.','No TPM action required.',{ tpm: value }) : warn(this.id,this.name,'TPM is not ready or could not be verified.','Enable TPM/fTPM/PTT in BIOS/UEFI if Delta Force or ACE requires it.',{ tpm: value, stderr: raw.stderr },false,'high'); } };
