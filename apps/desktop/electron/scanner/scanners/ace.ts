import type { Scanner } from '../types.js';
import { pass, warn } from '../result.js';
import { isWindows, runPowerShellJson } from '../../services/powershell.js';
export const aceScanner: Scanner = { id: 'ace', name: 'ACE Anti-Cheat', description: 'Checks for ACE anti-cheat services when present.', async run() { if (!isWindows()) return warn(this.id,this.name,'ACE service checks require Windows.','Run on the Windows gaming PC after installing Delta Force.',{ platform: process.platform },true); const { value, raw } = await runPowerShellJson<unknown[]>("Get-Service | Where-Object {$_.Name -match 'ACE|AntiCheat|Anti-Cheat' -or $_.DisplayName -match 'ACE|AntiCheat|Anti-Cheat'} | Select-Object Name,DisplayName,Status,StartType",[],15000); const services=Array.isArray(value)?value:[value].filter(Boolean); return raw.exitCode===0 && services.length>0 ? pass(this.id,this.name,'Anti-cheat related services were discovered.','If launch fails before the game window opens, repair or reinstall anti-cheat through official game files.',{ services },true) : warn(this.id,this.name,'No ACE anti-cheat service was discovered.','Install or verify Delta Force through the official launcher/Steam to restore anti-cheat components.',{ services, stderr: raw.stderr },true,'medium'); } };
