import type { Scanner } from '../types.js';
import { pass, warn } from '../result.js';
import { isWindows, runPowerShellJson } from '../../services/powershell.js';
export const gpuScanner: Scanner = { id: 'gpu', name: 'GPU', description: 'Inventories display adapters and installed driver versions.', async run() { if (!isWindows()) return warn(this.id,this.name,'GPU driver inventory requires Windows WMI.','Run on Windows to capture GPU driver details.',{ platform: process.platform },true); const { value, raw } = await runPowerShellJson<unknown[]>("Get-CimInstance Win32_VideoController | Select-Object Name,AdapterRAM,DriverVersion,DriverDate,PNPDeviceID",[],20000); const adapters=Array.isArray(value)?value:[value]; return raw.exitCode===0 && adapters.length>0 ? pass(this.id,this.name,'GPU driver inventory collected.','Use official NVIDIA, AMD, or Intel driver pages if crashes mention display drivers.',{ adapters },true) : warn(this.id,this.name,'No GPU adapter details were returned.','Reinstall the official GPU driver if display-related crashes continue.',{ stderr: raw.stderr },true); } };
