import type { Scanner } from '../types.js';
import { pass, warn } from '../result.js';
import { isWindows, runPowerShellJson } from '../../services/powershell.js';
export const biosScanner: Scanner = { id: 'bios', name: 'BIOS', description: 'Collects firmware vendor, version, and release date.', async run() { if (!isWindows()) return warn(this.id,this.name,'BIOS details require Windows WMI.','Check BIOS/UEFI settings on the Windows gaming PC.',{ platform: process.platform },false,'low'); const { value, raw } = await runPowerShellJson<Record<string, unknown>>("Get-CimInstance Win32_BIOS | Select-Object Manufacturer,SMBIOSBIOSVersion,ReleaseDate,SerialNumber",{},15000); return raw.exitCode===0 ? pass(this.id,this.name,'BIOS firmware details collected.','Update BIOS only from the motherboard or system vendor if firmware issues persist.',{ bios: value }) : warn(this.id,this.name,'Could not collect BIOS firmware details.','Review firmware version in System Information or BIOS setup.',{ stderr: raw.stderr },false,'low'); } };
