import type { Scanner } from '../types.js';
import { pass, warn } from '../result.js';
import { isWindows, runPowerShellJson } from '../../services/powershell.js';
export const driverScanner: Scanner = { id: 'driver', name: 'Driver', description: 'Inventories signed Plug and Play drivers relevant to launch stability.', async run() { if (!isWindows()) return warn(this.id,this.name,'Driver inventory requires Windows WMI.','Run on Windows to collect driver versions.',{ platform: process.platform },true); const { value, raw } = await runPowerShellJson<unknown[]>("Get-CimInstance Win32_PnPSignedDriver | Where-Object {$_.DeviceClass -in 'DISPLAY','MEDIA','NET','SYSTEM'} | Select-Object DeviceName,DeviceClass,DriverVersion,DriverDate,Manufacturer -First 80",[],30000); const drivers=Array.isArray(value)?value:[value]; return raw.exitCode===0 ? pass(this.id,this.name,'Relevant signed drivers were inventoried.','Update drivers only from official vendor utilities or support sites.',{ drivers },true) : warn(this.id,this.name,'Could not inventory signed drivers.','Use Device Manager or official vendor tools to validate drivers.',{ stderr: raw.stderr },true); } };
